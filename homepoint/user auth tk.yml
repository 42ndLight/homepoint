info:
  name: user auth tk
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/users/auth/token/
  body:
    type: json
    data: |-
      {
        "username": "spence",
        "password": "2m.b&CN*86VUT$*"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
